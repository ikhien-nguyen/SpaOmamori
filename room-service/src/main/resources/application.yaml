server:
  port: 8085
  servlet:
    context-path: /rooms

spring:
  application:
    name: room-service
  datasource:
    url: "jdbc:mysql://localhost:3306/omamori_room"
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 123456
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
