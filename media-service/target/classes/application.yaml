server:
  port: 8083
  servlet:
    context-path: /media

spring:
  application:
    name: media
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 5MB

# Kh�ng c� m?c spring.datasource / spring.jpa: service n�y kh�ng c� database ri�ng.
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

media:
  storage:
    # Th? m?c l?u file tr�n ??a, t??ng ??i so v?i working directory l�c ch?y service.
    # Khi ch?y b?ng Docker, mount volume v�o ?�ng th? m?c n�y ?? d? li?u kh�ng m?t
    # khi container b? recreate (VD: volumes: - media-data:/app/uploads).
    location: uploads
  # Base URL m� client (Frontend) s? d�ng ?? t?i ?nh v?, ?i qua API Gateway
  # (KH�NG tr? th?ng v�o port 8082) ?? ??ng nh?t domain v?i c�c API kh�c.
  public-base-url: http://localhost:8888/api/omamori/media/files
  allowed-content-types: image/jpeg,image/png,image/webp

jwt:
  signerKey: "${JWT_SIGNER_KEY:1TjXchw5FloESb63Kc+DFhTARvpWL4jUGCwfGWxuG5SIf/1y/LgJxHnMqaF6A/ij}"
