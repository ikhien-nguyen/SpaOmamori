server:
  port: 8081
  servlet:
    context-path: /profiles

spring:
  application:
    name: profiles
  datasource:
    url: "jdbc:mysql://localhost:3306/omamori_profiles"
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 123456
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/